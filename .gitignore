# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Local secrets (copy from .env.example)
.env

# Virtual environments
.venv

# Generated by `uv run model` (optional metadata / plots; model pkl is committed for deploy)
cardiovascular_model.threshold.json
feature_importance.png

# Firebase Admin SDK service account (local only; use Render secret for production)
firebase-admin.json

# Local Render MCP payloads (may contain secrets)
.render_*.json
